# FastAPI-
A full-stack Todo application built using FastAPI and React. The backend exposes RESTful CRUD APIs while the frontend includes both a React interface and a simple HTML client to manage todos.
